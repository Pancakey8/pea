dim arr(3, 5, 2)

for i = 1 to arr.length(1)
  for j = 1 to arr.length(2)
    for k = 1 to arr.length(3)
      let arr(i, j, k) = arr.dims() * (i + j + k)
    end for
  end for
end for

println(arr)

